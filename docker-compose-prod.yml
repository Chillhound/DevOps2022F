version: "3.9"

services:
  api:
    container_name: "api"
    image: axrez/devops-backend
    restart: on-failure
    ports:
      - 8080:80

  frontend:
    container_name: "frontend"
    image: axrez/devops-frontend
    ports:
      - 80:80
